diff --git a/app/globals.css b/app/globals.css
index 6f7780a..3c4e92a 100644
--- a/app/globals.css
+++ b/app/globals.css
@@ -1,12 +1,12 @@
 :root {
   color-scheme: dark;
   --bg: #03060e;
-  --header-bg: rgba(6, 10, 19, 0.85);
-  --panel: rgba(10, 15, 26, 0.75);
-  --panel-strong: rgba(15, 22, 38, 0.9);
-  --border: rgba(255, 255, 255, 0.07);
+  --header-bg: rgba(5, 8, 15, 0.92);
+  --panel: rgba(12, 16, 27, 0.92);
+  --panel-strong: rgba(16, 21, 34, 0.97);
+  --border: rgba(255, 255, 255, 0.09);
   --border-gold: rgba(194, 143, 44, 0.35);
-  --border-gold-light: rgba(241, 230, 195, 0.15);
+  --border-gold-light: rgba(241, 230, 195, 0.16);
   --border-blue: rgba(93, 169, 233, 0.3);
   --text: #f0f2f5;
   --muted: #8f9bb3;
@@ -41,16 +41,15 @@ h1, h2, h3, h4, h5, h6 {
   font-family: var(--font-display), "Outfit", ui-sans-serif, system-ui, sans-serif;
 }
 
-/* Ambient background glow: adds depth behind every page without competing with content */
+/* Ambient background glow: adds a quiet vignette without competing with content */
 .bgAmbient {
   position: fixed;
   inset: 0;
   z-index: -1;
   pointer-events: none;
   background:
-    radial-gradient(650px circle at 8% -5%, rgba(93, 169, 233, 0.14), transparent 60%),
-    radial-gradient(750px circle at 100% 10%, rgba(194, 143, 44, 0.1), transparent 60%),
-    radial-gradient(900px circle at 50% 120%, rgba(61, 220, 151, 0.05), transparent 60%);
+    radial-gradient(700px circle at 10% -10%, rgba(93, 169, 233, 0.08), transparent 60%),
+    radial-gradient(800px circle at 100% 0%, rgba(194, 143, 44, 0.06), transparent 60%);
 }
 
 /* Visible keyboard focus states for accessibility */
@@ -146,16 +145,16 @@ input, select {
 /* Card Glassmorphism Effect */
 .cardGlass {
   background: var(--panel);
-  backdrop-filter: blur(16px);
-  -webkit-backdrop-filter: blur(16px);
+  backdrop-filter: blur(8px);
+  -webkit-backdrop-filter: blur(8px);
   border: 1px solid var(--border);
-  border-radius: 12px;
-  box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.35);
-  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
+  border-radius: 10px;
+  box-shadow: 0 2px 14px 0 rgba(0, 0, 0, 0.25);
+  transition: all 0.2s ease;
 }
 
 .cardGlass:hover {
-  border-color: rgba(255, 255, 255, 0.12);
+  border-color: rgba(255, 255, 255, 0.14);
 }
 
 /* App container */
@@ -180,8 +179,8 @@ input, select {
   width: 100%;
   height: var(--header-height);
   background: var(--header-bg);
-  backdrop-filter: blur(16px);
-  -webkit-backdrop-filter: blur(16px);
+  backdrop-filter: blur(10px);
+  -webkit-backdrop-filter: blur(10px);
   border-bottom: 1px solid var(--border);
   z-index: 99;
 }
@@ -205,29 +204,27 @@ input, select {
 .logoMark {
   display: grid;
   place-items: center;
-  width: 40px;
-  height: 40px;
+  width: 38px;
+  height: 38px;
   border-radius: 8px;
   background: linear-gradient(135deg, var(--blue), var(--gold));
   color: #03060e;
-  box-shadow: 0 0 16px rgba(93, 169, 233, 0.3);
 }
 
 .logo strong {
   display: block;
-  font-size: 1.15rem;
-  letter-spacing: 0.5px;
-  background: linear-gradient(90deg, #fff, var(--gold-light));
-  -webkit-background-clip: text;
-  -webkit-text-fill-color: transparent;
+  font-size: 1.05rem;
+  font-weight: 700;
+  letter-spacing: 0.2px;
+  color: #fff;
 }
 
 .logo span {
   display: block;
-  font-size: 0.75rem;
+  font-size: 0.72rem;
   color: var(--gold);
   text-transform: uppercase;
-  letter-spacing: 1px;
+  letter-spacing: 1.2px;
 }
 
 .mainNav {
@@ -257,7 +254,6 @@ input, select {
   color: var(--gold-light);
   background: rgba(194, 143, 44, 0.1);
   border-color: var(--border-gold);
-  box-shadow: inset 0 0 10px rgba(194, 143, 44, 0.05);
 }
 
 /* Main Footer */
@@ -482,11 +478,11 @@ input, select {
 }
 
 h1 {
-  font-size: 2.2rem;
-  font-weight: 800;
-  letter-spacing: -0.5px;
+  font-size: 2.1rem;
+  font-weight: 700;
+  letter-spacing: -0.3px;
   margin: 0;
-  line-height: 1.15;
+  line-height: 1.2;
 }
 
 .deployLink {
